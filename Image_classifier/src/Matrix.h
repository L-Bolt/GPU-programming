#ifndef MATRIX
#define MATRIX

// #include <vector>
// #include <string>

// #include "util.h"

// class Matrix3D {
//     public:
//         Matrix3D(int rows, int columns, int channels, std::vector<uint8_t> &data);
//         ~Matrix3D() = default;

//         void set(int row, int column, int channel, uint8_t value);
//         uint8_t get(int row, int column, int channel);

//         int get_rows() const {return rows;};
//         int get_columns() const {return columns;};

//     private:
//         int rows;
//         int columns;
//         int channels;
//         std::vector<std::vector<std::vector<uint8_t>>> array;
// };

#endif
